This is just a simple test of environment variables.  The accompanying envs file is read and the env vars listed there are set.

They should also be cleared after the test.

	target readAndShowEnvVar
		env "SETME" => var
		show var
